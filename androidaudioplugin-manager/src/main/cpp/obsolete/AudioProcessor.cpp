/*
#include "AudioProcessor.h"

void
aap::AudioProcessor::initialize(int32_t sampleRate, int32_t audioInChannelCount, int32_t audioOutChannelCount, int32_t aapFrameSize, int32_t midiBufferSize) {
    // AAP settings
    sample_rate = sampleRate;
    buffer_size_in_samples = aapFrameSize;
    midi_buffer_size = midiBufferSize;
    audio_in_channel_count = audioInChannelCount;
    audio_out_channel_count = audioOutChannelCount;

    // Oboe configuration
    pal()->setupStream();

}
*/